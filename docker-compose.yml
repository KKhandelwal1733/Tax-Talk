# services:
#   qdrant:
#     image: qdrant/qdrant:latest
#     container_name: tax-talk-qdrant
#     restart: unless-stopped
#     ports:
#       - "6333:6333"  # HTTP API
#       - "6334:6334"  # gRPC
#     volumes:
#       - ./qdrant_storage:/qdrant/storage
#     environment:
#       QDRANT__SERVICE__HTTP_PORT: 6333
#       QDRANT__SERVICE__GRPC_PORT: 6334

  # Uncomment when you want to self-host Langfuse (week 4+).
  # For weeks 1-3, just use Langfuse Cloud free tier (cloud.langfuse.com).
  #
  # langfuse-db:
  #   image: postgres:16
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: langfuse
  #     POSTGRES_PASSWORD: langfuse_dev_password
  #     POSTGRES_DB: langfuse
  #   volumes:
  #     - ./langfuse_data:/var/lib/postgresql/data
  #
  # langfuse:
  #   image: langfuse/langfuse:latest
  #   restart: unless-stopped
  #   depends_on:
  #     - langfuse-db
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     DATABASE_URL: postgresql://langfuse:langfuse_dev_password@langfuse-db:5432/langfuse
  #     NEXTAUTH_SECRET: change-me-in-production
  #     NEXTAUTH_URL: http://localhost:3000
  #     SALT: change-me-too

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: tax-talk-api:latest
    container_name: tax-talk-api
    env_file:
      - .env
    environment:
      API_HOST: 0.0.0.0
      API_PORT: ${API_PORT:-8000}
      # Removed the internal docker hostname fallback since Qdrant is now in the cloud
      QDRANT_URL: ${QDRANT_URL}
      QDRANT_COLLECTION: ${QDRANT_COLLECTION:-gst_income_tax_contextual}
      # Note: Assuming gemini-1.5-flash unless you have early access to 3.1!
      CHAT_MODEL: ${CHAT_MODEL:-gemini-3.1-flash-lite}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      ENV: ${ENV:-production}
    ports:
      - "${API_PORT:-8000}:8000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health/live')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    deploy:
      resources:
        limits:
          # CRITICAL: Capped at 750m to leave ~250m for Ubuntu OS and Docker daemon
          memory: 750m
          cpus: "1.0"
        reservations:
          memory: 256m
          cpus: "0.25"
    networks:
      - tax-talk-net

networks:
  tax-talk-net:
    driver: bridge